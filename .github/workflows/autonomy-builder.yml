name: Autonomous Builder

on:
  issues:
    types:
      - labeled
  push:
    branches:
      - main
  schedule:
    - cron: "41 */6 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: Diagnostic mode; neither option invokes Codex or publishes a branch.
        required: true
        default: preflight
        type: choice
        options:
          - preflight
          - dry-run

permissions:
  contents: read
  issues: read
  pull-requests: read

concurrency:
  group: bounded-autonomous-builder
  cancel-in-progress: false

env:
  AUTONOMY_PAUSED: ${{ vars.AUTONOMY_PAUSED || 'true' }}
  AUTONOMY_APPROVERS: ${{ vars.AUTONOMY_APPROVERS || 'MuneebHoda' }}
  AUTONOMY_BUILDER_RECONCILE_ENABLED: ${{ vars.AUTONOMY_BUILDER_RECONCILE_ENABLED || 'false' }}

jobs:
  preflight:
    name: Builder preflight
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      execute_model: ${{ steps.preflight.outputs.execute_model }}
      issue_number: ${{ steps.preflight.outputs.issue_number }}
      trigger: ${{ steps.preflight.outputs.trigger }}
      context_sha256: ${{ steps.preflight.outputs.context_sha256 }}
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 2
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
      - name: Run deterministic preflight
        id: preflight
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
          GITHUB_TOKEN: ${{ github.token }}
          HAS_AUTONOMY_APP: ${{ secrets.AUTONOMY_APP_ID != '' && secrets.AUTONOMY_APP_PRIVATE_KEY != '' }}
          HAS_OPENAI_KEY: ${{ secrets.OPENAI_API_KEY != '' }}
        run: node scripts/autonomy/autonomy.mjs builder-preflight "$RUNNER_TEMP/builder-context"
      - name: Upload sanitized builder context
        if: steps.preflight.outputs.execute_model == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: builder-context
          path: ${{ runner.temp }}/builder-context
          retention-days: 1
          if-no-files-found: error

  claim:
    name: Claim approved issue
    needs: preflight
    if: needs.preflight.outputs.execute_model == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Download builder context
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: builder-context
          path: ${{ runner.temp }}/builder-context
      - name: Verify trusted builder context
        env:
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
        run: >-
          node scripts/autonomy/autonomy.mjs verify-context
          "$RUNNER_TEMP/builder-context/builder-context.json"
      - name: Create claim-only GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.AUTONOMY_APP_ID }}
          private-key: ${{ secrets.AUTONOMY_APP_PRIVATE_KEY }}
          owner: MuneebHoda
          repositories: MuneebHoda/RAG-Debugger
          permission-contents: read
          permission-issues: write
          permission-pull-requests: read
      - name: Claim one issue
        env:
          AUTONOMY_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: node scripts/autonomy/autonomy.mjs claim-issue "$RUNNER_TEMP/builder-context/builder-context.json"

  generate:
    name: Generate candidate
    needs:
      - preflight
      - claim
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Download builder context
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: builder-context
          path: ${{ runner.temp }}/builder-context
      - name: Preserve trusted automation
        run: |
          mkdir -p "$RUNNER_TEMP/trusted/.github" "$RUNNER_TEMP/trusted/scripts"
          cp -R .github/autonomy "$RUNNER_TEMP/trusted/.github/autonomy"
          cp -R scripts/autonomy "$RUNNER_TEMP/trusted/scripts/autonomy"
      - name: Verify trusted builder context
        env:
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          verify-context
          "$RUNNER_TEMP/builder-context/builder-context.json"
      - name: Prepare builder prompt
        env:
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          prepare-prompt builder
          "$RUNNER_TEMP/builder-context/builder-context.json"
          "$RUNNER_TEMP/builder-prompt.md"
      - name: Recheck pause before generation
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          assert-unpaused generation
      - name: Run bounded builder
        uses: openai/codex-action@52fe01ec70a42f454c9d2ebd47598f9fd6893d56 # v1.11
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt-file: ${{ runner.temp }}/builder-prompt.md
          output-file: ${{ runner.temp }}/builder-output.json
          output-schema-file: ${{ runner.temp }}/trusted/.github/autonomy/schemas/builder-output.schema.json
          working-directory: ${{ github.workspace }}
          sandbox: workspace-write
          safety-strategy: drop-sudo
          codex-version: 0.146.0
          model: gpt-5.6-sol
          effort: high
          allow-bots: true
          allow-bot-users: github-actions[bot]
      - name: Capture bounded candidate
        env:
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          capture-candidate
          "$RUNNER_TEMP/builder-output.json"
          "$RUNNER_TEMP/builder-context/builder-context.json"
          "$RUNNER_TEMP/candidate"
      - name: Upload untrusted candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: builder-candidate
          path: ${{ runner.temp }}/candidate
          retention-days: 1
          if-no-files-found: error
          include-hidden-files: true
          overwrite: false

  validate:
    name: Validate and seal candidate
    needs:
      - preflight
      - generate
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
      bundle_sha256: ${{ steps.seal.outputs.bundle_sha256 }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Preserve trusted automation
        run: |
          mkdir -p "$RUNNER_TEMP/trusted/.github" "$RUNNER_TEMP/trusted/scripts"
          cp -R .github/autonomy "$RUNNER_TEMP/trusted/.github/autonomy"
          cp -R scripts/autonomy "$RUNNER_TEMP/trusted/scripts/autonomy"
      - name: Download candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: builder-candidate
          path: ${{ runner.temp }}/candidate
      - name: Recheck pause before validation
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          assert-unpaused validation
      - name: Apply candidate safely
        env:
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs" apply-candidate "$RUNNER_TEMP/candidate"
      - name: Validate and seal candidate before code execution
        id: seal
        env:
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          seal-candidate
          "$RUNNER_TEMP/candidate"
          "$RUNNER_TEMP/candidate/attestation.json"
          "$RUNNER_TEMP/builder-sealed.json"
      - name: Upload immutable validated candidate
        id: upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: ${{ runner.temp }}/builder-sealed.json
          archive: false
          retention-days: 1
          if-no-files-found: error
          overwrite: false
      - name: Verify immutable upload digest
        env:
          LOCAL_DIGEST: ${{ steps.seal.outputs.bundle_sha256 }}
          UPLOAD_DIGEST: ${{ steps.upload.outputs.artifact-digest }}
        run: test "$LOCAL_DIGEST" = "$UPLOAD_DIGEST"

  quality:
    name: Run disposable candidate quality gate
    needs:
      - preflight
      - validate
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Preserve trusted automation
        run: |
          mkdir -p "$RUNNER_TEMP/trusted/.github" "$RUNNER_TEMP/trusted/scripts"
          cp -R .github/autonomy "$RUNNER_TEMP/trusted/.github/autonomy"
          cp -R scripts/autonomy "$RUNNER_TEMP/trusted/scripts/autonomy"
      - name: Download immutable candidate by artifact ID
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.validate.outputs.artifact_id }}
          path: ${{ runner.temp }}/sealed
          skip-decompress: true
          digest-mismatch: error
      - name: Revalidate and apply disposable candidate
        env:
          AUTONOMY_DOWNLOADED_ARTIFACT_DIGEST: ${{ needs.validate.outputs.artifact_digest }}
          AUTONOMY_DOWNLOADED_ARTIFACT_ID: ${{ needs.validate.outputs.artifact_id }}
          AUTONOMY_EXPECTED_ARTIFACT_DIGEST: ${{ needs.validate.outputs.bundle_sha256 }}
          AUTONOMY_EXPECTED_ARTIFACT_ID: ${{ needs.validate.outputs.artifact_id }}
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          revalidate-sealed-candidate
          "$RUNNER_TEMP/sealed/builder-sealed.json"
          "$RUNNER_TEMP/quality-candidate"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - name: Cache Rust
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Install Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - name: Install web dependencies
        working-directory: apps/web
        run: npm ci
      - name: Install Playwright
        working-directory: apps/web
        run: npx playwright install --with-deps chromium
      - name: Install pinned database and security tools
        run: |
          cargo install sqlx-cli --version 0.8.6 --no-default-features --features rustls,postgres --locked
          cargo install cargo-deny --version 0.20.2 --locked
      - name: Run trusted complete quality gate
        run: >-
          "$RUNNER_TEMP/trusted/scripts/autonomy/run-quality.sh"
          "$GITHUB_WORKSPACE"

  publish:
    name: Publish draft pull request
    needs:
      - preflight
      - validate
      - quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Preserve trusted automation
        run: |
          mkdir -p "$RUNNER_TEMP/trusted/.github" "$RUNNER_TEMP/trusted/scripts"
          cp -R .github/autonomy "$RUNNER_TEMP/trusted/.github/autonomy"
          cp -R scripts/autonomy "$RUNNER_TEMP/trusted/scripts/autonomy"
      - name: Download original immutable candidate by artifact ID
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.validate.outputs.artifact_id }}
          path: ${{ runner.temp }}/sealed
          skip-decompress: true
          digest-mismatch: error
      - name: Recheck pause before publisher validation
        env:
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          assert-unpaused validation
      - name: Revalidate and apply candidate without executing it
        env:
          AUTONOMY_DOWNLOADED_ARTIFACT_DIGEST: ${{ needs.validate.outputs.artifact_digest }}
          AUTONOMY_DOWNLOADED_ARTIFACT_ID: ${{ needs.validate.outputs.artifact_id }}
          AUTONOMY_EXPECTED_ARTIFACT_DIGEST: ${{ needs.validate.outputs.bundle_sha256 }}
          AUTONOMY_EXPECTED_ARTIFACT_ID: ${{ needs.validate.outputs.artifact_id }}
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          revalidate-sealed-candidate
          "$RUNNER_TEMP/sealed/builder-sealed.json"
          "$RUNNER_TEMP/publisher-candidate"
      - name: Recheck pause before publication
        env:
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          assert-unpaused publication
      - name: Create publication-only GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.AUTONOMY_APP_ID }}
          private-key: ${{ secrets.AUTONOMY_APP_PRIVATE_KEY }}
          owner: MuneebHoda
          repositories: MuneebHoda/RAG-Debugger
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write
      - name: Publish validated draft
        env:
          AUTONOMY_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
          AUTONOMY_REPOSITORY_ROOT: ${{ github.workspace }}
          AUTONOMY_TRUSTED_ROOT: ${{ runner.temp }}/trusted
        run: >-
          node "$RUNNER_TEMP/trusted/scripts/autonomy/autonomy.mjs"
          publish-builder
          "$RUNNER_TEMP/publisher-candidate"
          "$RUNNER_TEMP/publisher-candidate/attestation.json"

  block:
    name: Record blocked attempt
    needs:
      - preflight
      - claim
      - generate
      - validate
      - quality
      - publish
    if: ${{ always() && needs.preflight.outputs.execute_model == 'true' && needs.claim.result == 'success' && needs.publish.result != 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact trusted base
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Download builder context
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: builder-context
          path: ${{ runner.temp }}/builder-context
      - name: Verify trusted builder context
        env:
          AUTONOMY_EXPECTED_BASE_SHA: ${{ github.sha }}
          AUTONOMY_EXPECTED_CONTEXT_SHA256: ${{ needs.preflight.outputs.context_sha256 }}
          AUTONOMY_EXPECTED_ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
          AUTONOMY_EXPECTED_REPOSITORY: MuneebHoda/RAG-Debugger
          AUTONOMY_EXPECTED_RUN_URL: https://github.com/MuneebHoda/RAG-Debugger/actions/runs/${{ github.run_id }}
          AUTONOMY_EXPECTED_TRIGGER: ${{ needs.preflight.outputs.trigger }}
        run: >-
          node scripts/autonomy/autonomy.mjs verify-context
          "$RUNNER_TEMP/builder-context/builder-context.json"
      - name: Create issue-only GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.AUTONOMY_APP_ID }}
          private-key: ${{ secrets.AUTONOMY_APP_PRIVATE_KEY }}
          owner: MuneebHoda
          repositories: MuneebHoda/RAG-Debugger
          permission-contents: read
          permission-issues: write
      - name: Mark attempt blocked without retry
        env:
          AUTONOMY_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: >-
          node scripts/autonomy/autonomy.mjs block-issue
          "$RUNNER_TEMP/builder-context/builder-context.json"
          "generation, deterministic validation, or publication"
